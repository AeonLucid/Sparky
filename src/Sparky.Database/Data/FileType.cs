namespace Sparky.Database.Data
{
    public enum FileType
    {
        // Media
        MediaFile,
        MediaSubtitle,

        // Metadata
        MetadataPoster,
        MetadataBackground
    }
}
